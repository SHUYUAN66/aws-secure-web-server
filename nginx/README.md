# Nginx Config

This folder stores sample Nginx configuration files used in the deployment.

## Planned content

* `default.conf` — basic site config
* Optional: HTTPS redirect, security headers

> Reminder: Remove any private domains/IPs before publishing.
